// Fallback swatch for a tag with no entry in the tag catalog (public/data/tags.json,
// managed via /dev's Tag Manager) — e.g. a legacy tag that got removed there.
export const DEFAULT_TAG_META = { color: '#1e2a44' }
